package 高频题;

public class _887_鸡蛋掉落 {
    public int superEggDrop(int K, int N) {
        // dp[n] = dp[n - 1] || dp[n - 1] + 1;
        // i 层楼，j 个鸡蛋的最少次数
        // dp[i][j] =
        return 0;
    }
}
